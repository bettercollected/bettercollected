import HeaderModalWrapper from '@Components/Modals/ModalWrappers/HeaderModalWrapper';

export default function SuggestPriceForProModal({ props }: any) {

    return <HeaderModalWrapper showClose={false} className="max-w-[762px] w-full bg-white">

    </HeaderModalWrapper>;
}